<div class="flex flex-col gap-2 p-6 text-white bg-gray-200 shadow-md h-44 rounded-xl max-w-80 bg-opacity-30 backdrop-filter backdrop-blur-lg">
    <div class="text-lg font-semibold">{{ $title }}</div>
    <div class="flex justify-end text-5xl font-semibold tracking-tight">{{ $no }}</div>
    <div class="text-sm font-normal">{{ $desc }}</div>
</div>
